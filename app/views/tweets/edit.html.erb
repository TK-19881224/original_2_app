<h3>編集する</h3>
  <%= form_with(model: @tweet, local: true) do |form| %>
    <p><%= form.text_field :name, placeholder: "Nickname" %></p>
    <p><%= form.text_area :text, placeholder: "text", rows: "3 " %></p>
    <p><%= form.submit "SEND" %></p>
  <% end %>